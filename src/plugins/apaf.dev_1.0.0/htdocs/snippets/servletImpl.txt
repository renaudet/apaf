/*
 * ctx: {
 	"user": <connected user>,
 	"runtime": <npa runtime>,
 	"require": <Node.js 'require' function>,
 	"httpRequest": <original Express request object>,
 	"httpResponse": <original Express response object>
   }
   then: function(err,result)
 */
myFunction = function(params,ctx,then){
	then(null,{"status": "ok"});
}

servlet.endpoint = myFunction;